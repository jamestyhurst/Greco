Seed commentary references (Agadmator + SammyChess) + transcript fetcher

- commentary_refs/_tools/fetch_transcript.py: fetches a YouTube transcript as
  plain text (youtube-transcript-api), using the Windows certificate store for
  SSL (same workaround as narrator.py). Transcripts only; no video is downloaded.
- commentary_refs/agadmator-opera-game/: real Agadmator transcript of Morphy's
  Opera Game (channel verified via YouTube oEmbed author_name) + game.pgn + meta.
- commentary_refs/sammychess-kasparov-scotch/: SammyChess Kasparov-Scotch
  transcript + meta (multi-game compilation, so no single matching PGN).
- commentary_refs/README.md: document the automated fetcher + seeded references.

Verified: commentary.load_commentary_references() loads both as style-only
references (the system prompt forbids importing any board facts from them).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
