[pytest]
# Run the suite with:  venv\Scripts\python -m pytest
# pythonpath = . puts the repo root on sys.path so tests can import the top-level
# modules (outputs, triage, analyzer, knowledge) and the web/ and scripts/ packages.
pythonpath = .
testpaths = tests
addopts = -q
